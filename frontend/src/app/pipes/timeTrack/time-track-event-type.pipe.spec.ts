import { TimeTrackEventTypePipe } from './time-track-event-type.pipe';

describe('TimeTrackEventTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeTrackEventTypePipe();
    expect(pipe).toBeTruthy();
  });
});
