import { ExerciseTypePipe } from './exercise-type.pipe';

describe('ExerciseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
