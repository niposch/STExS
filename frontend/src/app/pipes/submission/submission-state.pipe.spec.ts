import { SubmissionStatePipe } from './submission-state.pipe';

describe('SubmissionStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionStatePipe();
    expect(pipe).toBeTruthy();
  });
});
