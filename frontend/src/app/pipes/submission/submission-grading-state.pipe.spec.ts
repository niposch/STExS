import { SubmissionGradingStatePipe } from './submission-grading-state.pipe';

describe('SubmissionGradingStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionGradingStatePipe();
    expect(pipe).toBeTruthy();
  });
});
