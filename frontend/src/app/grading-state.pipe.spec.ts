import { GradingStatePipe } from './grading-state.pipe';

describe('GradingStatePipe', () => {
  it('create an instance', () => {
    const pipe = new GradingStatePipe();
    expect(pipe).toBeTruthy();
  });
});
